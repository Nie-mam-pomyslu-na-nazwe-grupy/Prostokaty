package com.example.draganddrop;

import android.content.Context;
import android.graphics.Matrix;
import android.graphics.drawable.Drawable;
import android.view.View;
import android.view.ViewGroup;
import android.widget.ImageView;

import androidx.constraintlayout.solver.widgets.Rectangle;

import com.example.draganddrop.R;

import java.util.ArrayList;
/*
public class ImageBoard  extends ImageView {
    int dimX;
    int dimY;
    int gridSize;
    int startOfBoard;
    int topOfBoard;

    //konstruktor:
    public ImageBoard(Context context) {
        super(context);
    }

    ImageView imageBoard = findViewById(R.id.imageBoard);

    int BdimX = 20;
    int BdimY = 30;

    //pobieranie obrazu z ImageView
    ImageView imageBoard = findViewById(R.id.imageBoard);
    getBitmapPositionInsideImageView(imageBoard);

    //ustalanie zmiennych ImageBoard
    int[] dimensions = getBitmapPositionInsideImageView(imageBoard);
    int startOfBoard = dimensions[0];
    int topOfBoard = dimensions[1];
    int gridSize = dimensions[2] / dimX;//funkcja zakłada że kratki są idealnie kwadratowe

    //sendViewToBack(imageBoard);
    }



//przekopiowuje to z maina bo nie chce mi sie myslec jak tego uzyc bez kopiowania
*/